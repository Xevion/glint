FROM oven/bun:1-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    postgresql-client ca-certificates \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY scripts/backup.ts ./backup.ts

ENTRYPOINT ["bun", "run", "/app/backup.ts"]
