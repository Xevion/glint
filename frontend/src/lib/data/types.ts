// UI display types - simple re-exports from API for now
// Can add display enhancements later when backend provides more metadata

export type {
	Shader,
	ShaderVersion,
	Scene,
	World,
	Capture,
	CaptureWithContext,
	ShaderWithCaptures,
	SceneWithCaptures
} from '$lib/api/types';
